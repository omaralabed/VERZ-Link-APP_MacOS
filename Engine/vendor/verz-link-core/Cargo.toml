[package]
name = "verz-link-core"
version = "0.1.0"
edition = "2024"

[dependencies]
hmac = "0.12.1"
sha2 = "0.10.9"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
libc = "0.2"

[profile.release]
lto = true
codegen-units = 1
strip = true

